# 🚀 پاسخ به تسک ارزیابی: Multi-Agent Orchestration برای تولید محتوای ویدیویی

این پروژه در پاسخ به چالش طراحی و معماری سیستم‌های چندعاملی (Multi-Agent Agentic) برای تولید محتوای ویدیویی ساخته شده است. تمرکز اصلی ما بر **حفظ یکپارچگی داستان و کاراکتر** و همچنین **کنترل هزینه‌های عملیاتی** است.

---

### ۱. خلاصه چالش‌ها و راه‌حل‌های کلیدی

| چالش اصلی | راهکار استراتژیک |
| :--- | :--- |
| **ثبات کاراکتر** (Consistency) | استفاده از **حافظه مشترک ثابت (Shared Memory)** و یک **Agent داور (Checker)** برای اجرای قوانین کاراکتر. | 
| **مدیریت هزینه** | پیاده‌سازی منطق **تأییدیه سه‌گانه** و یک **Budget Supervisor Agent** برای جلوگیری از فراخوانی‌های گران و بی‌رویه API. |
| **هماهنگی زمانی** | تعریف **مسیر اجباری (Workflow)** با ابزارهایی مانند **LangGraph** و استفاده از خلاصه وضعیت Agent قبلی. | 
| **سوءتفاهم Agentها** | اجبار Agentها به ارتباط با زبان **کدگونه (JSON Schema)** به جای متن آزاد. |

---

### ۲. پیاده‌سازی PoC: ثبات کاراکتر و صرفه‌جویی

**هدف PoC:** نشان دادن چگونگی حفظ ثبات کاراکتر (Character Consistency) با استفاده از منطق حافظه مشترک و اجرای منطق صرفه‌جویی (تأییدیه سه‌گانه) قبل از انجام کار پرهزینه.

* **Edge Case حل شده:** در شبیه‌سازی، سیستم با موفقیت جلوی درخواست متناقض "لباس قرمز" را گرفت و کاراکتر را در "زره آبی سنگین" (طبق حافظه ثابت) نگه داشت.

### ۳. نحوه مشاهده و اجرای کد

این پروژه در پایتون نوشته شده و نیازی به نصب پکیج‌های خاص ندارد، اما برای سناریوی واقعی، وابستگی‌های لازم در `requirements.txt` ذکر شده است.

**دستورالعمل‌های اجرا:**

1.  فایل‌های پروژه را Clone یا دانلود کنید.
2.  کد را در محیط پایتون اجرا کنید:
    ```bash
    python poc_consistency_agentic.py
    ```
3.  خروجی کنسول را بررسی کنید؛ در صحنه دوم، **"هشدار تضاد"** را مشاهده خواهید کرد که نشان می‌دهد Agent بررسی ثبات، ثبات کاراکتر را **اجبار (Enforce)** کرده است.

### ۴. مستندات کامل

لطفاً فایل **`report.md`** را برای تحلیل عمیق‌تر چالش‌های Multi-Agent Orchestration، راه‌حل‌های ۴گانه پیشنهادی و جدول زمان‌بندی صرف‌شده مطالعه نمایید.
